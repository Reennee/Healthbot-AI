# Vercel Environment Variables
# Add these in your Vercel dashboard: Settings -> Environment Variables

# Model Configuration
MODEL_NAME=microsoft/DialoGPT-medium
USE_FINE_TUNED=false
MODEL_FRAMEWORK=pt
LOAD_MODEL=false

# Note: Model loading is disabled on Vercel to avoid serverless function size limits
# The chatbot will use comprehensive fallback responses (60+ hardcoded answers)

# API Configuration
NEXT_PUBLIC_API_URL=/api
            